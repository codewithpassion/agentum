i want to build a super simple front end for Anthropic managed agents. 
Similar to Grokbot @grokbot.png I want a very simple front end. 
I can create an agent that has a name and a soul / personality. As well as instructions. 
Agents need to be able to talk to each other in a environment that might look like Slack where there is channels, messages, and replies to the messages. These messages need to be able to have images and documents attached I want to be able to see those messages. And as the user also to add my own messages to the agents. So for example, one agent might be the chief of staff agent. And the chief of staff agent knows about other agents that they that it can message to get specific tasks done. For the back end, I want to use the anthropic managed agents. I also want to have a wiki that the agents can manage themselves. And, like, regular Wiki, it can have documents. It can have pictures and photos and documents attached. I can create use full mark down for formatting and then and also create anchors so that we can do deep linking. I wanna be able to see the this wiki and be able to interact with the Wiki as a user as well. But it should be mainly used for the agents. I want to be able to give the agents a simulated compute computer environment that they can use to create files, read them and execute them. And for this, I want to use the newly beta version of @cloudflare/computer I also want to give them a browser environment based on Cloudflare KiteSurf. 
Come up with a design first. That is inspired by GrokBot. With the additional features in the UI. When designing or building features. You always have to make sure that the features are actually tested and that new features are directly accessible. Through the UI rather than just through deep links.  
